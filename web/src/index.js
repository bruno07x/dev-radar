// Importação do React sempre é necessária quando houver elementos JSX (Javascript + HTML) EX => < App />
import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
// render => inserir dentro do DOM
ReactDOM.render(<App />, document.getElementById('root'));

